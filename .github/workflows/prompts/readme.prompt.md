# When updating the README.md in the root folder

- include a contents page at the top of the README
- Provide a summary of what this repo is by looking through the content/blog and content/ai folders, and insert this summary at the srart of the README
- Then provide succinct instructions as per the https://imfing.github.io/hextra/docs/getting-started/#setup-hextra-as-git-submodule on how we create a new hugo site using the hextra theme. Then include these additional points:
    - We are using the submodule implementation
    - We install the hugo.extended tool in windows 11 using winget -s winget
    - Include steps from initial install of the hextra theme all the way through to adding a new blog post (content/blog) with an image
    - Include instructions on how to add a menu main in th hugo.yaml file
- Include notes on how I've used github.copilot.chat.codeGeneration.instructions in the user settings to provide prompt context for the README.md file in root
- Please add a tree structure diagram detailing the folders and filess
